export interface Professional {
  id: string;
  name: string;
  slug: string;
  specialty: string;
  description: string;
  image: string;
  location: string;
  district: string;
  rating: number;
  reviewCount: number;
  verified: boolean;
}